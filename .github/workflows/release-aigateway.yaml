name: Release AI Gateway Chart

on:
  workflow_dispatch:
    inputs:
      version:
        description: Private Chart Version (required, e.g. 2.2.0)
        required: true
        type: string

jobs:
  release:
    uses: ./.github/workflows/release-private-chart.yaml
    with:
      name: aigateway
      version: ${{ inputs.version }}
    secrets: inherit
