name: Release AI Firewall Chart

on:
  workflow_dispatch:
    inputs:
      version:
        description: Private Chart Version (required, e.g. 0.1.5)
        required: true
        type: string

jobs:
  release:
    uses: ./.github/workflows/release-private-chart.yaml
    with:
      name: ai-firewall
      version: ${{ inputs.version }}
    secrets: inherit
